rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Helper to check if any user or anonymous read is allowed
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
